# Temp / editor
*.tmp
*~
.DS_Store
Thumbs.db

# Office lock files
~$*

# Scratch drafts that must never be mistaken for approved content
docs/scratch_*.md
**/*.draft.md

# Dependencies / build output (once a stack is chosen — see docs/stack.md)
node_modules/
dist/
build/
.env
.env.local
